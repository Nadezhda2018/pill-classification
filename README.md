# 💊 Классификатор медицинских препаратов (Pill Classifier)

Проект по автоматической классификации таблеток на изображениях с использованием глубокого обучения. В основе лежит архитектура **ResNet18** и метод **Transfer Learning**.

## 📊 О проекте
Данная модель разработана для распознавания различных типов лекарственных средств. Основная сложность задачи заключается в визуальном сходстве многих препаратов, что требует тонкой настройки финальных слоев нейросети.

**Ключевые характеристики:**
- **Библиотеки:** PyTorch, Torchvision, Scikit-learn, Pandas.
- **Архитектура:** ResNet18 (Pre-trained on ImageNet).
- **Точность (Accuracy):** >75% (на валидационной выборке).

---

## 📂 Структура проекта
```text
project/
├── data/               # Датасеты (train/test) — не включены в репозиторий
├── models/             # Сохраненные веса моделей (.pt) -- не включены в репозиторий
├── src/                # Модульный исходный код
│   ├── __init__.py
│   ├── data_utils.py      # Загрузка и предобработка данных
│   ├── model.py        # Архитектура нейросети
│   ├── train_utils.py  # Цикл обучения и валидации
│   └── eval.py         # Оценка качества и метрики
├── solution.ipynb      # Основной ноутбук с экспериментами
├── requirements.txt    # Зависимости проекта
└── .gitignore          # Правила исключения временных файлов